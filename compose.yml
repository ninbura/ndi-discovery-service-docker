services:
  ndi-discovery-server:
    container_name: ndi-discovery-server
    image: ninbura/ndi-discovery-server
    ports:
      - 5959:5959
    restart: unless-stopped
    # environment:
      # - NDI_PORT=5959
      # - NDI_BIND_IP=0.0.0.0
